function PropsDisplay({ name, age }) {

    return (
        <div className="card">
            <h3>Props Display</h3>
            <p>My Name is {name} and I'm {age} years old.</p>
        </div>
    );
    
};

export default PropsDisplay;